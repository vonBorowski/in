<% content_for :title, "Editar #{@task.name}" %>
<% breadcrumb :edit_task, @task %>

<%= bootstrap_form_for @task, layout: :horizontal, label_col: "col-sm-2", control_col: "col-sm-10" do |f| %>
  <%= render partial: "form", locals: { f: f } %>
<% end %>