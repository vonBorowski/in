<% content_for :title, "Editar #{@project.name}" %>
<% breadcrumb :edit_project, @project %>

<%= bootstrap_form_for @project, layout: :horizontal, label_col: "col-sm-2", control_col: "col-sm-10" do |f| %>
  <%= render partial: "form", locals: { f: f } %>
<% end %>