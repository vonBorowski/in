<% content_for :title, "Novo Lançamento" %>
<% breadcrumb :new_time_entry, @user %>

<%= bootstrap_form_for @time_entry, url: user_time_entry_index_path(@user), layout: :horizontal, label_col: "col-sm-2", control_col: "col-sm-10" do |f| %>
  <%= render partial: "form", locals: { f: f } %>
<% end %>